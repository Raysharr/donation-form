import styled from 'styled-components';

const MoneyInputStartIconContainer = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
  padding-left: ${({ theme }) => theme.spacings.sm}px;
`;

export default MoneyInputStartIconContainer;
